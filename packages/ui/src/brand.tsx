// The brand layer lives in two modules so each name is exported once:
// - icons.tsx: Logo, LogoMark, Icon, Glyph, GlyphTile and LOGO_SLANT, the
//   ribbon's 1-in-6 slant that every brand drawing shares (also served by
//   the "@storevia/ui/icons" subpath, which apps already import);
// - illustrations.tsx: empty-state Illustration and BusinessScene art.
// docs/design/design-plan.md §6 describes the system.
export {};
